node_modules/
dist/
.expo/
.expo-shared/
*.log
npm-debug.*
.DS_Store
*.tsbuildinfo

# Native build artifacts if ever ejected
ios/
android/

# Editor
.vscode/
.idea/

# Local assistant tooling config (machine-specific, not project source)
.claude/

# Regeneratable data pipeline outputs (rerun scripts/wikidata_enrich.py and
# scripts/connections_generator.py to reproduce; only the shipped .gz is tracked)
data/enriched_hot100.csv
data/connections.json

